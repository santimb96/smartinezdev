const Layout = ({ children }) => {
  return (
    <section className='w-screen md:w-[65vw] flex flex-col gap-[10px] p-[20px] border border-slate-600'>{children}</section>
  )
}

export default Layout
